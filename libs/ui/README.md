# ui

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test ui` to execute the unit tests via [Vitest](https://vitest.dev/).

## Library structure upgrade

The UI code has been reorganized under `src/lib` to follow Nx library conventions. Paths for components, hooks, utils, themes, and assets are now grouped and can be imported using the `@defi-token/ui` alias. Update your import statements accordingly.
